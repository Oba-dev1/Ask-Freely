# ============================================================================
# Environment Variables Template
# ============================================================================
#
# SECURITY NOTICE:
# All Firebase credentials MUST be set via environment variables.
# There are NO hardcoded fallbacks in the code for security reasons.
#
# For local development:
#   1. Copy this file to .env.local (which is gitignored)
#   2. Fill in your Firebase credentials
#
# For Netlify deployment:
#   1. Go to Site Settings > Environment Variables
#   2. Add each variable below with your production values
# ============================================================================

# Environment (development, staging, production)
REACT_APP_ENV=development

# Firebase Configuration (REQUIRED)
# Get these from: Firebase Console > Project Settings > Your Apps
REACT_APP_FIREBASE_API_KEY=your-api-key-here
REACT_APP_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
REACT_APP_FIREBASE_DATABASE_URL=https://your-project-default-rtdb.firebaseio.com/
REACT_APP_FIREBASE_PROJECT_ID=your-project-id
REACT_APP_FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
REACT_APP_FIREBASE_APP_ID=your-app-id

# Google reCAPTCHA v3 (REQUIRED for bot protection)
# Get this from: https://www.google.com/recaptcha/admin
REACT_APP_RECAPTCHA_SITE_KEY=your-recaptcha-site-key-here

# Resend Email API (OPTIONAL - for email notifications)
# Get this from: https://resend.com/api-keys
REACT_APP_RESEND_API_KEY=your-resend-api-key-here

# ============================================================================
# SECURITY REMINDERS:
# - NEVER commit .env files with real credentials
# - NEVER hardcode API keys in source code
# - Rotate API keys immediately if exposed
# - Use .env.local for local development (gitignored)
# - reCAPTCHA Secret Key should NEVER be in frontend code
# ============================================================================
